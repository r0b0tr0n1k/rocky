---
id: TASK_0003
state: completed
phase: done
created_at: 2026-07-21T01:10:21.608Z
updated_at: 2026-07-21T19:50:08.881Z
title: Produce a concrete implementation plan (roadmap, priority ordering, dependency graph, and resource estimates) for every work order (WO) listed in `apps/docs/content/workorder.md` whose current status is **not** "Done", "Done ✅", "Done ✓", "In Progress", or "In Progress ⏳". This includes WOs with status "Open", "Deferred", "Draft", "Open · answer pending …", as well as feature gaps (WO-164–WO-173) that lack any status annotation. The plan must account for cross-WO dependencies (e.g. WO-123 phases depend on WO-105/WO-089), external-blocker WOs (ADR-0081/0061 pending legal/gov answers), and deferred-by-design items. The output must be a single document (or set of documents) written to the repo, e.g. `apps/docs/content/workorder-plan.md`, that an engineering team can execute sprint by sprint.
label: Non-Done WO plan from workorder.md
---

## raw prompt

plan unrealised workorders @apps/docs/content/workorder.md

## refined prompt

GOAL
  Produce a concrete implementation plan (roadmap, priority ordering, dependency graph, and resource estimates) for every work order (WO) listed in `apps/docs/content/workorder.md` whose current status is **not** "Done", "Done ✅", "Done ✓", "In Progress", or "In Progress ⏳". This includes WOs with status "Open", "Deferred", "Draft", "Open · answer pending …", as well as feature gaps (WO-164–WO-173) that lack any status annotation. The plan must account for cross-WO dependencies (e.g. WO-123 phases depend on WO-105/WO-089), external-blocker WOs (ADR-0081/0061 pending legal/gov answers), and deferred-by-design items. The output must be a single document (or set of documents) written to the repo, e.g. `apps/docs/content/workorder-plan.md`, that an engineering team can execute sprint by sprint.

CONSTRAINTS
  - Do not change the status, description, or source ADR of any existing WO in `apps/docs/content/workorder.md` — the plan is a derived artifact, not a mutation of the source of truth.
  - Preserve every WO identifier (WO-XXX) and every concrete ADR cross-reference (ADR-00XX) verbatim; map each unrealised WO to the exact section/table in the source document where it is defined.
  - Honour the priority (P0–P3 / Future) and the "answer pending" designations in the source — the plan must clearly distinguish between execution-blocked WOs (waiting on gov/legal input) and execution-ready WOs.
  - Keep the "Deferred" WOs (WO-061, WO-070, etc.) in the plan but clearly segregated as non-sprint items with their deferral rationale preserved.
  - Do not fabricate implementation details that are not derivable from the source workorder.md or from the existing on-disk codebase (the project files shown in PROJECT ORIENTATION are authoritative for what exists).
  - The plan may propose a sprint/phase ordering, but must never override the priority column in the source — if the source says P3, the plan cannot schedule it as P0.
  - Every unrealised WO must appear in the plan; none may be silently dropped.

KNOWN-UNKNOWNS
  - WO-071 (calving-gap divergence) says "needs domain-owner confirmation" — is that confirmation now available, or should the plan treat the divergence as unresolved and flag the WO as blocked?
  - WO-123 Phase 4 ("`check:web-parity` guardian") has no spec — what exactly should the guardian check (diff `api-reference.mdx` TOC vs import tree, or a runtime test, or a static analysis rule)?
  - The User Guide Feature Gaps (WO-164–WO-173) have no "Status" column and no link to a backlog — are they to be treated as P-level feature requests requiring a new ADR and implementation, or as documentation-only updates (i.e. the feature already exists but the doc page is missing)?
  - Several "Draft" WOs (WO-111, WO-112, WO-117, WO-122) are unaccepted — should the plan include an "ADR-authoring" step for each before execution, or is the plan only for accepted work?
  - WO-145 (Secret management prod) says "prod deferred" — is there a target sprint, or is it indefinitely parked?
  - The `rocky_rls_test` constrained role for RLS tests exists only on the dev DB; does the plan need to script its creation for CI environments, or is CI expected to run only the skip-eligible tests?

EXTERNAL-DEPENDENCIES
  - Expo (Expo Push Notification API, EAS Build)  Mobile push notification delivery and cloud build/submission service for Android/iOS
  - Cloudflare (Cloudflare Access, Cloudflare Secrets Store, Cloudflare Tunnel)  Zero-trust identity-aware proxy, secrets vault, and encrypted tunnel for deployment topology
  - PostGIS (PostgreSQL spatial extension)  Spatial data storage and query extension for geographic features (geofences, admin_units, addresses POINT)
  - OpenStreetMap (Nominatim API)  Open geodata reverse-geocoding service used for telemetry/display in WO-112
  - Twilio / Vonage (SMS delivery API)  Third-party SMS gateway for notification channel routing (WO-156, deferred)
  - Copernicus Sentinel-2 (ESA satellite imagery)  Satellite data source referenced for EUDR deforestation overlay (WO-143 context, not a direct API integration)
  - TRACES NT (European Commission)  EU veterinary certification system targeted for CHED-compliant export in WO-121

## verified tooling

pnpm lint
pnpm --filter web lint
pnpm --filter docs lint
pnpm --filter web check-types
pnpm --filter docs check-types
pnpm test
pnpm --filter @rocky/api test
pnpm --filter web test
pnpm --filter @rocky/mobile-app test
pnpm build
pnpm --filter @rocky/api build
pnpm --filter web build
pnpm --filter docs build
pnpm test:e2e
docker compose up
docker build .
pnpm dev

## research

FILES
Now I have all the information needed. Let me compile the complete FILES section.

APIS
WO-024  FIELD_CHANGED → VD approval lock (P3, Open, Master List, ADR-0027)
WO-025  QR-code-scannable ear tags (P3, Open, Master List, ADR-0024 §E / 0009 §6)
WO-034  Scaffold @rocky/observability + TraceStage/MetricsStage (P2, Open, Master List, ADR-0020 P2 / 0003)
WO-035  Retire manual logger.* → span attributes (P3, Open, Master List, ADR-0020 P3)
WO-036  Business metrics emitted inside services/workers (P3, Open, Master List, ADR-0020 P3)
WO-060  Gate IoT behind RuleSet.features.iot (P3, Open, Master List, ADR-0031)
WO-061  IoT LPWAN QoS/SLA model — deferred (Future, Deferred, Master List, ADR-0031)
WO-070  Deferral register: AMR, buffer, lineage, blockchain, SMS (Future, Deferred, Master List, ADR-0023/0014)
WO-071  Confirm animal calving-gap divergence 365d vs 120d (P3, Open, Master List, ADR-0025)
WO-086  Add i18n layer (session.language, centralize strings, dir/RTL) (P2, Open, Master List, ADR-0040)
WO-097  Infrastructure/device admin parity + device-sync touchpoint (P2, Open, Master List, ADR-0047)
WO-111  addresses.location varchar → PostGIS POINT (4326) (P3, Draft, Geo, ADR-0053)
WO-112  OSM boundaries doc + Nominatim reverse-geocode (P3, Draft, Geo, ADR-0053)
WO-117  GDPR pseudonymization on subject exit (P2, Draft, Geo, ADR-0054/0061)
WO-122  GDPR public-health exception — Art.6(1)(c)+9(2)(i) (P2, Draft, Geo, ADR-0054/0061)
WO-124  Ratify Tier-1 invariant set — blocks hard-reject (P1, Open·answer pending gov, ADR-0081)
WO-125  Off-system-buyer disease-control trace (P1, Open·pending design, ADR-0081)
WO-126  Suspicious-eartag notify-someone procedure (P2, Open·TBD, ADR-0081)
WO-127  Enforce 8-hour live→slaughterhouse transport window (P2, Open, ADR-0081)
WO-128  Slaughterhouse tag-return obligation tracking (P2, Open, ADR-0081)
WO-129  Vet processes lost/destroyed eartag procedure (P2, Open, ADR-0081)
WO-130  Movement validator: nullable toFarmId + external counterparty (P1, Open·pending ratification, ADR-0081)
WO-131  Carcass/meat tracking scope (P3, Open·TBD, ADR-0081)
WO-132  EarTag takeover file: user-supplied takeoverId vs derive-from-order (P3, Open, ADR-0081)
WO-133  GDPR applicability scope decision (P0, Open·answer pending legal, ADR-0061)
WO-134  Enumerate MK national acts → gdpr.overrides[] (P1, Open·pending legal input, ADR-0061)
WO-135  DSAR identity verification (P1, Open, ADR-0061)
WO-136  Break-glass emergency PII access (P1, Open, ADR-0061)
WO-137  Signing-key custody + continuous verification (P2, Open, ADR-0061)
WO-138  Pseudonym rotation on erasure (P2, Open, ADR-0061)
WO-139  Erasure ≠ deletion: crypto-shred DEK + sever bridge (P2, Open, ADR-0061)
WO-140  GDPR consent-fantasy / graceful degradation (P1, Open·answer pending realism, ADR-0061)
WO-141  GDPR vs ISO 27701: do not architect to voluntary standard (P1, Open, ADR-0061)
WO-142  Frontend visibility/permission model for accept-and-flag UI (P1, Open·modeled, ADR-0081)
WO-145  Secret management — prod deferred (Open·prod deferred, Deployment, ADR-0083)
WO-161  Geo geometry helpers relocated to @rocky/geo (Open, Visa-Matrix, ROCKY-DS 001:2026(E))
WO-164  Bulk birth registration (P2, User Guide Gap, GAP-001 / animals/birth-registration.mdx)
WO-165  Tag re-order from farm without admin intervention (P1, User Guide Gap, GAP-002 / animals/ear-tag-orders.mdx)
WO-166  Pending-movements dashboard with 7-day countdown (P1, User Guide Gap, GAP-003 / movements/index.mdx)
WO-167  Push notification for nearby notifiable disease (P2, User Guide Gap, GAP-004 / health/notifiable-diseases.mdx)
WO-168  Vaccination schedule / calendar view (P3, User Guide Gap, GAP-005 / health/vaccinations.mdx)
WO-169  Farmer-facing risk-score dashboard (P2, User Guide Gap, GAP-006 / inspections/risk-analysis.mdx)
WO-170  In-app correction request status tracking (P3, User Guide Gap, GAP-007 / corrections/index.mdx)
WO-171  Role-based home screen — farmer vs vet (P1, User Guide Gap, GAP-008 / getting-started/navigation.mdx)
WO-172  Cross-border movement pre-approval workflow (P2, User Guide Gap, GAP-009 / movements/import-export.mdx)
WO-173  Offline queue status indicator with pending count (P3, User Guide Gap, GAP-010 / account/offline-sync.mdx)
pnpm check:trpc-boundary  scripts/check-trpc-boundary.mjs — fails if generated client has ReturnType< or backend imports
pnpm check:web-parity  scripts/check-web-parity.mjs — diff api-reference TOC vs apps/web trpc usage (WO-123 Phase 4)
pnpm check:layers  scripts/check-layers.mjs — machine-check Visa-Matrix (Annex C, WO-160 enforcement)
pnpm check:adrs  scripts/check-adrs.mjs — verify ADR cross-references and DoD
pnpm check:md-links  scripts/check-md-links.mjs — verify markdown link integrity
pnpm check:standards  scripts/check-standards.mjs — compliance standards check
pnpm check:agents  scripts/check-agents.mjs — verify AGENTS.md contracts
pnpm check:pdfa  pnpm --filter @rocky/pdf check:pdfa — PDF/A compliance tests
pnpm check:shadcn  scripts/check-shadcn-primitives.mjs — shadcn UI primitive check
pnpm generate:trpc  nestjs-trpc generate + patch-trpc-transformer — regenerate tRPC client
pnpm generate:enums  node scripts/regenerate-enums.mjs — regenerate enum schemas from constants
pnpm generate:zod  node scripts/generate-dumb-zod.mjs — regenerate Dumb Zod layer
pnpm generate:all  regenerate-enums + generate-dumb-zod
pnpm ci:checks  full CI gate: generate:trpc + check:trpc-boundary + adrs + md-links + standards + agents + layers + shadcn + pdfa + web-parity + test
pnpm dev  turbo run dev — start all dev servers
pnpm build  turbo run build — build all packages
pnpm test  turbo run test — run all tests
apps/docs/content/workorder.md  source of truth for all WO definitions, statuses, priorities, source ADRs
apps/docs/content/ADR/  directory containing all ADR documents (ADR-0001 through ADR-0109)
apps/docs/content/ADR/ADR-TEMPLATE.md  template for new ADR creation
apps/docs/content/ADR/INDEX.md  index of all ADRs
apps/docs/content/Standardization/diamond-seal-type-contracts.md  Diamond Seal type contracts standard (ROCKY-DS 001:2026(E))
scripts/check-layers.mjs  Visa-Matrix layer boundary enforcement script
scripts/check-web-parity.mjs  web↔backend parity guardian script
scripts/check-trpc-boundary.mjs  tRPC generated-client boundary guard

CONTEXT
- `check:web-parity.mjs` already exists, is wired into `ci:checks`, and passes in non-strict mode (router-coverage gate, mutation-level `--strict` not enforced). WO-123 Phase 4's guardian is partially implemented — the plan's Phase 4 work is to flip `--strict` on, not invent the script.

- `farm_org_id()` SECURITY DEFINER function exists only on the live dev DB (`192.168.1.109/tbot`) and in `scripts/fix-rls-sql.mjs` as an injection; it is NOT CREATED by any durable code file (`rls-helpers.ts` only references it via `sql\`farm_org_id(...)\``). A fresh `db-recreate.sh` without the fix-script loses the function — the plan must include either absorbing it into `rls-helpers.ts` or formally enshrining the fix-script as the durable migration path.

- `subjects` table has `RLS ENABLED` but **zero policies** → deny-all for non-superuser: any Vet or VD staff querying the `subjects` table directly (not through the API layer) gets zero rows, though the API's RLS-scoped `SubjectRepository` uses superuser connections and skips this bug. The plan must treat this as a latent production reliability gap, not a correctness gap.

- Seed data has zero `addresses.commune_id` values and zero `org_areas` rows, so the `farm_org_id()` → `current_org_id` branch of every org-scoped RLS policy returns NULL — org-area-based VET/VD access is **non-functional at the data level** even though the policy code is correct. Farmers still work via `farm_subjects` self-link. The plan must include a seed-data fix somewhere (WO-031's deferred data-seed track).

- `ci:checks` is a root `pnpm` script with **no GitHub Actions or `.github/workflows`** directory — CI orchestration is absent. The plan must treat any CI-gated step (WO-102 drift guard, WO-107 turbo coverage, WO-123 Phase 4 `--strict` enforcement) as relying on manual `pnpm ci:checks` invocation until CI infra is committed.

- WO-082's only remaining gate is **native verification** (impossible in CI-less harness; `expo-sqlite` crashes on web via `SharedArrayBuffer`). The plan should treat this as "blocked on native build infrastructure" and not schedule it in a CI-verifiable sprint.

- The `rocky_rls_test` NOSUPERUSER role exists only on the one dev DB (`192.168.1.109`). Every RLS test suite uses `describe.skipIf(!hasRLSEnv)` which skips silently when the env+role are absent. The plan does NOT need to script role creation for CI — the `skipIf` pattern already handles env absence — but must document that RLS assertions only execute against a properly-provisioned Postgres.

- `EXPO_ACCESS_TOKEN` is absent from every `.env` and `.env.example`; the Expo push client (`sendExpoPush`) silently returns when it's missing. WO-091's server-side emit is dead code until this env var is set. The plan must schedule obtaining an EAS project access token before WO-091 can be verified end-to-end.

- ADR-0081 WOs (WO-124–WO-132) and ADR-0061 WOs (WO-133–WO-141) all share Status "Open · answer pending (gov)"/"Open · answer pending (legal)". **No code can be written** — the plan must segregate them into a non-execution backlog gated on an external-status check, not schedule them as sprint items.

- WO-156 (notification channel routing) has `ChannelRouter` + tests Done but worker + SMS client are deferred ("at the end"). The plan's remaining scope for WO-156 is the `acknowledgedAt` column + mobile `confirmDelivery` + delivery worker + real SMS client (Twilio/Vonage) — three distinct sub-tasks with different dependency chains.

- The `api-reference.mdx` (auto-generated) currently lists **25 routers / 183 procedures**, while ADR-0055 and the how-to reference 24/156. The parity script's `ROUTER_EXEMPT` defaults to `sync` only — `geo` is NOT exempt and must have a web page (it does: `/admin/geo`). The plan's Phase 4 `--strict` enforcement must reconcile these counts or the gate will fail.

- `drizzle-studio` compose service requires `DRIZZLE_MASTER_PASS` env var at runtime, but this var is absent from `.env.example` and the `db-recreate.sh` workflow. Any sprint that involves inspecting the DB via Drizzle Studio must first add the env var.

- WO-024 (`FIELD_CHANGED` → VD lock) and WO-025 (QR ear tags) are P3/Open with no concrete spec beyond the parenthetical in the ADR — the plan must treat them as requiring ADR-authoring before any implementation sprint.

- The "Draft" WOs (WO-111 address location→POINT, WO-112 OSM/Nominatim, WO-117 GDPR pseudonymization, WO-122 GDPR public-health exception) are listed in a "Draft — not yet in Master List" block — the plan must include an ADR-authoring step for each before any execution, and the ADR-authoring itself is the deliverable.

- WO-145 "prod deferred" has no target sprint — the plan should park it as Future indefinitely, matching the source's explicit lack of commitment.

VERIFIED-TOOLING
  pnpm lint
  pnpm --filter web lint
  pnpm --filter docs lint
  pnpm --filter web check-types
  pnpm --filter docs check-types
  pnpm test
  pnpm --filter @rocky/api test
  pnpm --filter web test
  pnpm --filter @rocky/mobile-app test
  pnpm build
  pnpm --filter @rocky/api build
  pnpm --filter web build
  pnpm --filter docs build
  pnpm test:e2e
  docker compose up
  docker build .
  pnpm dev

## grill Q&A

Q1: For the User Guide Feature Gaps (WO-164–WO-173) listed with GAP identifiers and doc paths but no Status column — do these features already exist in the codebase but lack published documentation (making each a docs-only sprint), or are they unimplemented features requiring full backend/frontend development before any docs can be written?
A1: treat as feature requests requiring full development lifecycle (ADR authoring, backend/frontend implementation, then docs) — the source labels them as P-level priority items alongside code WOs and does not confirm the underlying feature exists anywhere in the codebase
Q2: For the four Draft WOs (WO-111, WO-112, WO-117, WO-122) that are listed as "Draft — not yet in Master List" with existing ADRs but no acceptance, should the plan include a formal ADR-acceptance step for each as the first deliverable (making those WOs "ADR ratification" sprints before any implementation), or should the plan treat them as execution-ready once their ADRs are finalized (i.e., proceed directly to implementation if the ADR is already written)?
A2: Include a formal ADR-ratification step for each Draft WO (WO-111, WO-112, WO-117, WO-122) as the first deliverable before any implementation, because the research explicitly states that ADR-authoring is the deliverable and must precede execution (auto)
Q3: For WO-071 (calving-gap divergence, P3), the source says "needs domain-owner confirmation" — is that confirmation now available (making the WO execution-ready), or should the plan treat the 365d vs 120d divergence as unresolved and flag WO-071 as blocked pending external input?
A3: treat the divergence as unresolved and flag WO-071 as blocked pending domain-owner confirmation (auto)
Q4: For the farm_org_id() SECURITY DEFINER function — the research found it exists only via the live dev DB and scripts/fix-rls-sql.mjs injection, but is absent from any durable source file — should the plan propose absorbing it into @rocky/db/rls-helpers.ts as a formal migration (code change, reviewable, versioned), or keep it as an enshrined fix-script (script-only, simpler but unversioned in DB schema)?
A4: absorb into @rocky/db/rls-helpers.ts as formal migration
Q5: The research identifies two production-reliability gaps not covered by any existing WO: (i) the subjects table has RLS enabled with zero policies (deny-all for non-superusers), and (ii) seed data has zero addresses.commune_id and zero org_areas values, making org-scoped RLS policies non-functional for VET/VD access at the data level. Should the plan document these as known risks only (consistent with "do not fabricate new deliverables"), or include actionable steps within the closest related in-scope WO's planning section (e.g., WO-097's infrastructure/device-admin track for the RLS policy gap)?
A5: document as known risks only (consistent with the constraint against fabricating WO scope not derivable from the source) (auto)
Q6: WO-123 Phase 4 (flip --strict on check:web-parity.mjs): the research says "the plan's Phase 4 work is to flip --strict on, not invent the script," but also notes the script's router-count and procedure-count expectations (25/183 vs 24/156) and ROUTER_EXEMPT list (sync only, geo not exempt) will cause the gate to fail under --strict — does Phase 4 scope include (A) reconciling those counts and the exempt list first, then flipping --strict, or (B) simply flipping the flag as a one-line config change, documenting that the gate will fail until the count discrepancy is resolved by other work outside Phase 4?
A6: reconcile counts and exempt list first, then flip --strict (auto)
Q7: For WO-145 (Secret management prod, status: "Open · prod deferred"), should the plan assign it to a specific target sprint (and if so, which one), or treat it as indefinitely parked in Future — matching the source's explicit lack of commitment to a timeline?
A7: park it as Future indefinitely, matching the source's explicit lack of commitment to a timeline (auto)

## spec

GOAL
Produce a concrete implementation plan (roadmap, priority ordering, dependency graph, and resource estimates) for every work order (WO) listed in `apps/docs/content/workorder.md` whose current status is **not** "Done", "Done ✅", "Done ✓", "In Progress", or "In Progress ⏳". This includes WOs with status "Open", "Deferred", "Draft", "Open · answer pending …", as well as feature gaps (WO-164–WO-173) that lack any status annotation. The plan must account for cross-WO dependencies (e.g. WO-123 phases depend on WO-105/WO-089), external-blocker WOs (ADR-0081/0061 pending legal/gov answers), and deferred-by-design items. The output is a single document written to `apps/docs/content/workorder-plan.md` that an engineering team can execute sprint by sprint.

CONSTRAINTS
- Do not change the status, description, or source ADR of any existing WO in `apps/docs/content/workorder.md` — the plan is a derived artifact, not a mutation of the source of truth.
- Preserve every WO identifier (WO-XXX) and every concrete ADR cross-reference (ADR-00XX) verbatim; map each unrealised WO to the exact section/table in the source document where it is defined.
- Honour the priority (P0–P3 / Future) and the "answer pending" designations in the source — the plan must clearly distinguish between execution-blocked WOs (waiting on gov/legal input) and execution-ready WOs.
- Keep the "Deferred" WOs (WO-061, WO-070, etc.) in the plan but clearly segregated as non-sprint items with their deferral rationale preserved.
- Do not fabricate implementation details that are not derivable from the source workorder.md or from the existing on-disk codebase (the project files shown in PROJECT ORIENTATION are authoritative for what exists).
- The plan may propose a sprint/phase ordering, but must never override the priority column in the source — if the source says P3, the plan cannot schedule it as P0.
- Every unrealised WO must appear in the plan; none may be silently dropped.
- WO-071 (calving-gap divergence) must be treated as blocked pending domain-owner confirmation (unresolved), consistent with Q3.
- Draft WOs (WO-111, WO-112, WO-117, WO-122) require a formal ADR-ratification step as the first deliverable before any implementation, consistent with Q2.
- User Guide Feature Gaps (WO-164–WO-173) must be treated as full feature requests requiring the complete lifecycle (ADR authoring → backend/frontend implementation → documentation), consistent with Q1.
- The `farm_org_id()` SECURITY DEFINER function (currently injected only via `scripts/fix-rls-sql.mjs` and the live dev DB) must be absorbed into `@rocky/db/rls-helpers.ts` as a formal migration, consistent with Q4.
- WO-123 Phase 4 scope must include reconciling router-count and procedure-count expectations and the `ROUTER_EXEMPT` list before flipping `--strict` on `check:web-parity.mjs`, consistent with Q6.
- ADR-0081 WOs (WO-124–WO-132) and ADR-0061 WOs (WO-133–WO-141) are all blocked on external legal/gov input — segregate into a non-execution backlog, not sprint items.
- WO-145 ("prod deferred") must be parked as Future indefinitely with no target sprint assigned, consistent with Q7.
- WO-024 (`FIELD_CHANGED` → VD lock) and WO-025 (QR ear tags) are P3/Open with no concrete spec — treat as requiring ADR-authoring before any implementation sprint.
- The two production-reliability gaps (subjects table RLS enabled with zero policies, seed data missing addresses.commune_id and org_areas) must be documented as known risks only — no actionable steps added, consistent with Q5.
- CI orchestration is absent (no `.github/workflows` directory); any CI-gated step in the plan (WO-102 drift guard, WO-107 turbo coverage, WO-123 Phase 4 `--strict` enforcement) relies on manual `pnpm ci:checks` invocation until CI infra is committed.
- WO-082 is blocked on native build infrastructure (SharedArrayBuffer crash on web); schedule only after native CI environment exists.
- `EXPO_ACCESS_TOKEN` is absent from all `.env` files; WO-091 server-side emit is dead code until this env var is set.
- Drizzle Studio compose service requires `DRIZZLE_MASTER_PASS` env var at runtime, absent from `.env.example` — document as prerequisite.
- The `rocky_rls_test` role exists only on the one dev DB; RLS assertions use `describe.skipIf(!hasRLSEnv)` and silently skip when env is absent — the plan does not need to script role creation for CI but must document this constraint.

ACCEPTANCE
- `apps/docs/content/workorder-plan.md` exists and is valid Markdown.
- Every unrealised WO (Open, Draft, Deferred, Feature Gap) appears in the plan, each mapped to its source section/table, priority, and ADR cross-reference.
- Execution-ready WOs are clearly segregated from external-blocker and deferred-by-design WOs, with blocking conditions explicitly stated.
- Sprint/phasing order respects the source priority column — no P3 item is scheduled ahead of a P0 or P1 item.
- The plan includes a **dependency graph** showing inter-WO relationships (including cross-WO dependencies such as WO-123 phases depending on WO-105/WO-089).
- The plan includes **resource estimates** (team size, sprint count, or person-weeks, as appropriate for each WO).
- Every Draft WO and unspecced P3 WO includes an "ADR-authoring" or "ADR-ratification" step as the first deliverable.
- The external-dependency register lists all seven validated dependencies (Expo, Cloudflare, PostGIS, OpenStreetMap, Twilio/Vonage, Copernicus Sentinel-2, TRACES NT) with their role and status.
- The plan references the Visa-Matrix layer-boundary enforcement (`scripts/check-layers.mjs`) and the `pnpm ci:checks` composite gate as the verification mechanism for all code-changed WOs.
- The `farm_org_id()` migration step appears in the closest sprint (alongside or immediately following WO-097's RLS scope) and is labelled as a formal migration into `@rocky/db/rls-helpers.ts`.
- The docs site builds without errors after inserting the plan document.

VERIFY:
```sh
set -e
echo "=== 1. Plan file exists ==="
test -f apps/docs/content/workorder-plan.md

echo "=== 2. WO reference count ==="
grep -coP '^WO-\d+' apps/docs/content/workorder-plan.md || true

echo "=== 3. Dependency graph present ==="
grep -qi 'dependency graph\|dependency.*graph\|graph.*depend' apps/docs/content/workorder-plan.md || { echo "FAIL: no dependency graph section"; exit 1; }

echo "=== 4. Resource estimates present ==="
grep -qi 'resource estimat\|person.weeks\|sprint count\|team size' apps/docs/content/workorder-plan.md || { echo "FAIL: no resource estimates"; exit 1; }

echo "=== 5. Cross-WO dependencies documented ==="
grep -qi 'cross.*depend.*WO\|depends on.*WO-\|WO-.*depends on\|WO-.*→.*WO-\|WO-.*blocks.*WO-' apps/docs/content/workorder-plan.md || { echo "FAIL: no cross-WO dependencies"; exit 1; }

echo "=== 6. WO-071 flagged as blocked ==="
grep -qi 'WO-071.*blocked\|blocked.*WO-071\|WO-071.*unresolved\|WO-071.*pending.*confirm\|WO-071.*domain.owner.*pend' apps/docs/content/workorder-plan.md || { echo "FAIL: WO-071 not marked blocked"; exit 1; }

echo "=== 7. farm_org_id migration in plan ==="
grep -qi 'farm_org_id.*migrat\|migrat.*farm_org_id.*rls-helpers\|absorb.*farm_org_id' apps/docs/content/workorder-plan.md || { echo "FAIL: farm_org_id migration not mentioned"; exit 1; }

echo "=== 8. External dependency register contains all seven ==="
grep -qi 'Expo' apps/docs/content/workorder-plan.md || { echo "FAIL: Expo missing from dependency register"; exit 1; }
grep -qi 'Cloudflare' apps/docs/content/workorder-plan.md || { echo "FAIL: Cloudflare missing from dependency register"; exit 1; }
grep -qi 'PostGIS\|Postgres.*spatial\|spatial.*extens' apps/docs/content/workorder-plan.md || { echo "FAIL: PostGIS missing from dependency register"; exit 1; }
grep -qi 'OpenStreetMap\|Nominatim' apps/docs/content/workorder-plan.md || { echo "FAIL: OpenStreetMap missing from dependency register"; exit 1; }
grep -qi 'Twilio\|Vonage.*SMS\|SMS.*gateway' apps/docs/content/workorder-plan.md || { echo "FAIL: Twilio/Vonage missing from dependency register"; exit 1; }
grep -qi 'Copernicus.*Sentinel\|Sentinel-2\|satellite.*EUDR\|deforestation.*overlay' apps/docs/content/workorder-plan.md || { echo "FAIL: Copernicus Sentinel-2 missing from dependency register"; exit 1; }
grep -qi 'TRACES.*NT\|TRACES NT\|EU.*veterinary.*cert\|CHED.*export' apps/docs/content/workorder-plan.md || { echo "FAIL: TRACES NT missing from dependency register"; exit 1; }

echo "=== 9. Docs site builds ==="
pnpm --filter docs build 2>&1

echo "=== All checks passed ==="
```

## phase timings

refine               78.4s
research            968.7s
  enrichment              2.2s
  worker:files wait       1.5s
  worker:files work     136.5s
  worker:apis wait        1.6s
  worker:apis work      130.6s
  worker:context wait     1.5s
  worker:context work   109.7s
  worker:tooling wait     1.5s
  worker:tooling work    66.8s
  workers               931.9s
  verify-tooling         36.8s
grill              1138.8s
  gen                    28.8s
  auto-answer            36.4s
  gen                   155.0s
  auto-answer            81.2s
  gen                    41.2s
  auto-answer            22.3s
  gen                    35.5s
  auto-answer            34.3s
  gen                   117.7s
  auto-answer            45.3s
  gen                    37.4s
  auto-answer            99.6s
  gen                   128.9s
  auto-answer            13.1s
  gen                   146.2s
compose              35.9s
critique            251.4s
  triage                146.8s
  rewrite               104.6s
total              2473.2s

## handoff

handoff_at: 2026-07-21T01:51:59.325Z

## gates

- 2026-07-21T01:58:01.407Z verify: FAIL — repo health: `bun run lint` exited 2
- 2026-07-21T02:09:03.666Z lint-fix: applied — re-verifying
- 2026-07-21T02:18:51.172Z verify: FAIL — verify child mutated repo state and its verdict was discarded (state restored: restored modified file apps/docs/next-env.d.ts)
- 2026-07-21T02:24:12.406Z resolution: recommended ACCEPT
- 2026-07-21T19:50:08.723Z resolution: user ACCEPTED the work despite verify FAIL
- 2026-07-21T19:50:08.877Z commit: task snapshot committed

