---
id: TASK_0002
state: failed
phase: done
created_at: 2026-07-20T18:38:27.270Z
updated_at: 2026-07-20T19:03:23.949Z
title: Create the farmer and veterinarian user guide documentation section in the existing Nextra docs site (`apps/docs/`), following the plan at `plans/2026-07-20-farmer-vet-docs/plan.md`. Done means: the directory `apps/docs/content/user-guide/` exists with all subdirectories and MDX pages as specified in the plan's folder structure (Section 3.2), the `apps/docs/content/_meta.ts` is updated to insert the User Guide section after "Get Started" and before the "Learn" separator (Section 3.1), the `apps/docs/content/user-guide/_meta.ts` navigation config matches Section 3.3 verbatim, every workflow page includes the corresponding Mermaid diagram from Section 4, every page follows the template from Section 6 (title frontmatter, Mermaid diagram, "When to Do This", "Requirements", "Step-by-Step", "What Happens Next", and a "Feature Gap" section with `<!-- GAP: -->` markers from Section 5), and `pnpm build` passes with no errors.
label: Farmer & Vet User Guide (Nextra)
---

## raw prompt

Lets create the documentation as planned File: plans/2026-07-20-farmer-vet-docs/plan.md

## refined prompt

GOAL
Create the farmer and veterinarian user guide documentation section in the existing Nextra docs site (`apps/docs/`), following the plan at `plans/2026-07-20-farmer-vet-docs/plan.md`. Done means: the directory `apps/docs/content/user-guide/` exists with all subdirectories and MDX pages as specified in the plan's folder structure (Section 3.2), the `apps/docs/content/_meta.ts` is updated to insert the User Guide section after "Get Started" and before the "Learn" separator (Section 3.1), the `apps/docs/content/user-guide/_meta.ts` navigation config matches Section 3.3 verbatim, every workflow page includes the corresponding Mermaid diagram from Section 4, every page follows the template from Section 6 (title frontmatter, Mermaid diagram, "When to Do This", "Requirements", "Step-by-Step", "What Happens Next", and a "Feature Gap" section with `<!-- GAP: -->` markers from Section 5), and `pnpm build` passes with no errors.

CONSTRAINTS
- Do not modify any existing file outside `apps/docs/content/user-guide/` except `apps/docs/content/_meta.ts`, which must be updated in-place to add the User Guide section (insert after `'get-started': 'Get Started'` and before `'###learn': { type: 'separator', title: 'Learn' }`) while preserving every existing entry, separator, and ordering.
- The `apps/docs/content/user-guide/_meta.ts` must match Section 3.3 of the plan exactly — every key, title, type, and items structure.
- Every MDX page must include the exact `<!-- GAP: -->` marker and gap summary table row from Section 5 for the gaps relevant to that page.
- Every workflow page must include the Mermaid diagram from Section 4 that corresponds to that page's topic.
- Every page must follow the template in Section 6: frontmatter with `title` and `sidebarTitle`, `import { Mermaid } from 'nextra/mermaid'`, `# Page Title`, `<Mermaid chart={...} />`, `## When to Do This`, `## Requirements`, `## Step-by-Step`, `## What Happens Next`, `---`, `### Feature Gap`.
- Do not add any new npm packages or dependencies — the plan states no new packages are needed.
- Do not create landing pages, PDF export, or a separate site.
- All content must be in English (Macedonian translation is a follow-up, not part of this task).
- Do not modify any existing file in `apps/docs/` other than `apps/docs/content/_meta.ts`.
- Preserve every existing dependency, devDependency, script, and field in all `package.json` files — no changes to any `package.json`.

KNOWN-UNKNOWNS
- Should the gap markers be placed only on pages that have a corresponding gap in Section 5's table, or should every page get a `<!-- GAP: -->` marker even if no gap is listed? The plan shows gaps for specific pages only; clarify whether pages without a listed gap should omit the Feature Gap section entirely or include a placeholder.
- The plan lists `ear-tag-orders.mdx` under `animals/` in the folder structure (Section 3.2) but also as a standalone TODO item (TODO-farmer-vet-docs-010). Should it live at `user-guide/animals/ear-tag-orders.mdx` (as the folder structure implies) or at `user-guide/ear-tag-orders.mdx` (as the TODO phrasing "standalone page or inside animals/" suggests)? The folder structure is authoritative — confirm.
- The plan's Section 4.2 shows an Ear Tag Order Lifecycle state diagram. Which page should host it — `animals/ear-tag-orders.mdx` or `animals/index.mdx`? The plan does not explicitly map each diagram to a page.
- The plan's Section 4.6 shows a Cattle Passport Lifecycle state diagram. Which page should host it — `passports/index.mdx` or `passports/apply.mdx`?
- The plan's Section 4.5 shows an Inspection Risk Analysis flowchart. Which page should host it — `inspections/risk-analysis.mdx` or `inspections/index.mdx`?

EXTERNAL-DEPENDENCIES

## verified tooling

(verification inconclusive)

## research

FILES
apps/docs/content/_meta.ts  Root navigation config — must be edited in-place to insert User Guide section after 'get-started' and before '###learn'
apps/docs/content/user-guide/_meta.ts  Sub-navigation config — must be created matching Section 3.3 verbatim
apps/docs/content/user-guide/index.mdx  User Guide landing/overview page
apps/docs/content/user-guide/getting-started/_meta.ts  Getting Started sub-navigation config
apps/docs/content/user-guide/getting-started/index.mdx  Welcome & system overview
apps/docs/content/user-guide/getting-started/account-setup.mdx  Register, sign in, sign out
apps/docs/content/user-guide/getting-started/navigation.mdx  App tour (tabs, offline indicator) — hosts GAP-008
apps/docs/content/user-guide/animals/_meta.ts  Animals sub-navigation config
apps/docs/content/user-guide/animals/index.mdx  Animals section overview
apps/docs/content/user-guide/animals/birth-registration.mdx  Register a new birth — hosts GAP-001; hosts Section 4.1 Mermaid diagram
apps/docs/content/user-guide/animals/ear-tag-orders.mdx  Order & collect ear tags — hosts GAP-002; hosts Section 4.2 Mermaid diagram
apps/docs/content/user-guide/animals/managing-animals.mdx  Search, view details, correct animals
apps/docs/content/user-guide/movements/_meta.ts  Movements sub-navigation config
apps/docs/content/user-guide/movements/index.mdx  When must you record a movement? — hosts GAP-003; hosts Section 4.3 Mermaid diagram
apps/docs/content/user-guide/movements/death.mdx  Record animal death (on-farm, in-transit, at slaughter)
apps/docs/content/user-guide/movements/pasture.mdx  Pasture movement
apps/docs/content/user-guide/movements/slaughter.mdx  Send to slaughter
apps/docs/content/user-guide/movements/market.mdx  Market/sale movement
apps/docs/content/user-guide/movements/import-export.mdx  Cross-border movements — hosts GAP-009
apps/docs/content/user-guide/health/_meta.ts  Health sub-navigation config
apps/docs/content/user-guide/health/index.mdx  Health management overview — hosts Section 4.4 Mermaid diagram
apps/docs/content/user-guide/health/vaccinations.mdx  Record a vaccination — hosts GAP-005
apps/docs/content/user-guide/health/treatments.mdx  Record treatment/diagnosis
apps/docs/content/user-guide/health/lab-tests.mdx  Lab test requests & results
apps/docs/content/user-guide/health/notifiable-diseases.mdx  Disease alerts & mandatory reporting — hosts GAP-004
apps/docs/content/user-guide/passports/_meta.ts  Passports sub-navigation config
apps/docs/content/user-guide/passports/index.mdx  Cattle passport lifecycle — hosts Section 4.6 Mermaid diagram
apps/docs/content/user-guide/passports/apply.mdx  Request a passport
apps/docs/content/user-guide/passports/seized.mdx  Seized/lost/damaged passport
apps/docs/content/user-guide/inspections/_meta.ts  Inspections sub-navigation config
apps/docs/content/user-guide/inspections/index.mdx  Inspection system overview
apps/docs/content/user-guide/inspections/risk-analysis.mdx  How risk scoring works — hosts GAP-006; hosts Section 4.5 Mermaid diagram
apps/docs/content/user-guide/inspections/on-spot-inspection.mdx  What happens during an inspection
apps/docs/content/user-guide/corrections/_meta.ts  Corrections sub-navigation config
apps/docs/content/user-guide/corrections/index.mdx  When & how to correct errors — hosts GAP-007
apps/docs/content/user-guide/account/_meta.ts  Account sub-navigation config
apps/docs/content/user-guide/account/index.mdx  Profile, settings, notifications overview
apps/docs/content/user-guide/account/notifications.mdx  Alert types & preferences
apps/docs/content/user-guide/account/offline-sync.mdx  How offline mode works — hosts GAP-010
apps/docs/content/user-guide/web-admin/_meta.ts  Web Admin sub-navigation config
apps/docs/content/user-guide/web-admin/index.mdx  Admin panel overview
apps/docs/content/user-guide/web-admin/dashboard.mdx  Dashboard & KPIs
apps/docs/content/user-guide/web-admin/managing-farms.mdx  Farm CRUD & keeper management
apps/docs/content/user-guide/web-admin/managing-users.mdx  Subject roles & permissions
apps/docs/content/user-guide/web-admin/audit-tools.mdx  Audit log, compliance
apps/docs/content/user-guide/web-admin/system-settings.mdx  Feature flags, parameters
plans/2026-07-20-farmer-vet-docs/plan.md  The contract for this task — defines folder structure, nav config, diagrams, gaps, and page template

APIS
Now I have full context. Let me compile the APIS.

`apps/docs/content/_meta.ts`  Must be edited in-place to insert User Guide section after `'get-started'` and before `'###learn'`; must preserve all existing entries, separators, and ordering

`import type { Meta } from 'nextra'`  Type import for all `_meta.ts` files — used in root `_meta.ts` and every subdirectory `_meta.ts`

`type: 'separator'`  Property on a `_meta.ts` entry to render a visual separator line with a title (used for `'###user-guide'` and `'###learn'`)

`type: 'menu'`  Property on a `_meta.ts` entry to create an expandable sub-menu with `items` (used for every section in `user-guide/_meta.ts`)

`apps/docs/content/user-guide/_meta.ts`  Must be created matching Section 3.3 of the plan verbatim — 9 top-level menu entries (`index`, `getting-started`, `animals`, `movements`, `health`, `passports`, `inspections`, `corrections`, `account`, `web-admin`) each with `title`, `type: 'menu'`, and `items` listing child pages

`apps/docs/content/user-guide/index.mdx`  User Guide landing/overview page — must be created with title frontmatter and overview content

`apps/docs/content/user-guide/getting-started/_meta.ts`  Sub-nav config for Getting Started section — must be created with `index`, `account-setup`, `navigation` items

`apps/docs/content/user-guide/getting-started/index.mdx`  Welcome & system overview — must be created

`apps/docs/content/user-guide/getting-started/account-setup.mdx`  Register/sign in/sign out — must be created

`apps/docs/content/user-guide/getting-started/navigation.mdx`  App tour (tabs, offline indicator) — hosts GAP-008; must be created

`apps/docs/content/user-guide/animals/_meta.ts`  Sub-nav config for Animals section — must be created with `index`, `birth-registration`, `ear-tag-orders`, `managing-animals` items

`apps/docs/content/user-guide/animals/index.mdx`  Animals section overview — must be created

`apps/docs/content/user-guide/animals/birth-registration.mdx`  Register a new birth — hosts GAP-001; hosts Section 4.1 Mermaid diagram; must be created

`apps/docs/content/user-guide/animals/ear-tag-orders.mdx`  Order & collect ear tags — hosts GAP-002; hosts Section 4.2 Mermaid diagram; must be created

`apps/docs/content/user-guide/animals/managing-animals.mdx`  Search, view details, correct animals — must be created

`apps/docs/content/user-guide/movements/_meta.ts`  Sub-nav config for Movements section — must be created with `index`, `death`, `pasture`, `slaughter`, `market`, `import-export` items

`apps/docs/content/user-guide/movements/index.mdx`  When must you record a movement? — hosts GAP-003; hosts Section 4.3 Mermaid diagram; must be created

`apps/docs/content/user-guide/movements/death.mdx`  Record animal death — must be created

`apps/docs/content/user-guide/movements/pasture.mdx`  Pasture movement — must be created

`apps/docs/content/user-guide/movements/slaughter.mdx`  Send to slaughter — must be created

`apps/docs/content/user-guide/movements/market.mdx`  Market/sale movement — must be created

`apps/docs/content/user-guide/movements/import-export.mdx`  Cross-border movements — hosts GAP-009; must be created

`apps/docs/content/user-guide/health/_meta.ts`  Sub-nav config for Health section — must be created with `index`, `vaccinations`, `treatments`, `lab-tests`, `notifiable-diseases` items

`apps/docs/content/user-guide/health/index.mdx`  Health management overview — hosts Section 4.4 Mermaid diagram; must be created

`apps/docs/content/user-guide/health/vaccinations.mdx`  Record a vaccination — hosts GAP-005; must be created

`apps/docs/content/user-guide/health/treatments.mdx`  Record treatment/diagnosis — must be created

`apps/docs/content/user-guide/health/lab-tests.mdx`  Lab test requests & results — must be created

`apps/docs/content/user-guide/health/notifiable-diseases.mdx`  Disease alerts & mandatory reporting — hosts GAP-004; must be created

`apps/docs/content/user-guide/passports/_meta.ts`  Sub-nav config for Passports section — must be created with `index`, `apply`, `seized` items

`apps/docs/content/user-guide/passports/index.mdx`  Cattle passport lifecycle — hosts Section 4.6 Mermaid diagram; must be created

`apps/docs/content/user-guide/passports/apply.mdx`  Request a passport — must be created

`apps/docs/content/user-guide/passports/seized.mdx`  Seized/lost/damaged passport — must be created

`apps/docs/content/user-guide/inspections/_meta.ts`  Sub-nav config for Inspections section — must be created with `index`, `risk-analysis`, `on-spot-inspection` items

`apps/docs/content/user-guide/inspections/index.mdx`  Inspection system overview — must be created

`apps/docs/content/user-guide/inspections/risk-analysis.mdx`  How risk scoring works — hosts GAP-006; hosts Section 4.5 Mermaid diagram; must be created

`apps/docs/content/user-guide/inspections/on-spot-inspection.mdx`  What happens during an inspection — must be created

`apps/docs/content/user-guide/corrections/_meta.ts`  Sub-nav config for Corrections section — must be created with `index` item only

`apps/docs/content/user-guide/corrections/index.mdx`  When & how to correct errors — hosts GAP-007; must be created

`apps/docs/content/user-guide/account/_meta.ts`  Sub-nav config for Account section — must be created with `index`, `notifications`, `offline-sync` items

`apps/docs/content/user-guide/account/index.mdx`  Profile, settings, notifications overview — must be created

`apps/docs/content/user-guide/account/notifications.mdx`  Alert types & preferences — must be created

`apps/docs/content/user-guide/account/offline-sync.mdx`  How offline mode works — hosts GAP-010; must be created

`apps/docs/content/user-guide/web-admin/_meta.ts`  Sub-nav config for Web Admin section — must be created with `index`, `dashboard`, `managing-farms`, `managing-users`, `audit-tools`, `system-settings` items

`apps/docs/content/user-guide/web-admin/index.mdx`  Admin panel overview — must be created

`apps/docs/content/user-guide/web-admin/dashboard.mdx`  Dashboard & KPIs — must be created

`apps/docs/content/user-guide/web-admin/managing-farms.mdx`  Farm CRUD & keeper management — must be created

`apps/docs/content/user-guide/web-admin/managing-users.mdx`  Subject roles & permissions — must be created

`apps/docs/content/user-guide/web-admin/audit-tools.mdx`  Audit log, compliance — must be created

`apps/docs/content/user-guide/web-admin/system-settings.mdx`  Feature flags, parameters — must be created

```` ```mermaid ````  Fenced code block for Mermaid diagrams — actual project pattern (DO NOT use `nextra/mermaid` component); set via `@theguild/remark-mermaid` which is already installed and automatically renders these blocks

`title` (frontmatter)  Required frontmatter key on every MDX page — value is the full page title

`sidebarTitle` (frontmatter)  Required frontmatter key on every MDX page — value is the short navigation label

`## When to Do This`  Required section heading in every workflow page (Section 6 template)

`## Requirements`  Required section heading in every workflow page (Section 6 template)

`## Step-by-Step`  Required section heading in every workflow page (Section 6 template)

`## What Happens Next`  Required section heading in every workflow page (Section 6 template)

`---`  Horizontal rule required before the Feature Gap sub-section (Section 6 template)

`### Feature Gap`  Required sub-section heading at bottom of pages with gaps (Section 6 template)

`<!-- GAP-XXX: description -->`  HTML comment marker for each gap in Section 5 table — placed in the Feature Gap section of the corresponding page

`pnpm dev`  Run `pnpm --filter docs dev` to start the docs dev server on port 3002

`pnpm build`  Run `pnpm --filter docs build` to verify no build errors — MUST pass before task is done

`pnpm check:md-links`  Run via `pnpm --filter docs ...` or root `pnpm check:md-links` to verify all internal doc links resolve

CONTEXT
Here are the architectural facts gathered from the repo that will change HOW the agent implements this task:

- **Mermaid diagrams use fenced code blocks, NOT `<Mermaid chart={...} />`** — the project uses `@theguild/remark-mermaid@^0.3.0` which renders from ` ```mermaid ` fences. The `mdx-components.tsx` has no Mermaid component. The how-to page at `how-to/use-mermaid-diagrams.mdx` explicitly says "no client component, no `<Mermaid>` wrapper, no extra import." The agent MUST use fenced ` ```mermaid ` blocks, NOT the plan's `import { Mermaid } from 'nextra/mermaid'` pattern.

- **Root `_meta.ts` structure uses `type: 'separator'` with `title` property** — separators use `{ type: 'separator', title: 'User Guide' }` syntax. The insertion target is between `'get-started': 'Get Started'` (line 6) and `'###learn': { type: 'separator', title: 'Learn' }` (line 9).

- **Existing index/landing pages use `asIndexPage: true` frontmatter** — see `content/how-to/index.mdx` and `content/tutorials/index.mdx`. If the plan's Section 3.2 folder structure pages like `animals/index.mdx`, `movements/index.mdx`, etc. exist, they should include `asIndexPage: true` based on the existing pattern (the plan template in Section 6 does not include it, but the existing project uses it).

- **`pnpm check:md-links` validates internal links** — the guardian script at `scripts/check-md-links.mjs` resolves every `[text](url)` and rejects `../` escapes. Any internal doc links in the new pages must use absolute paths from `content/` (e.g., `/how-to/use-mermaid-diagrams`), never `../` escapes.

- **Nextra 4.6.1 + Next 16.2.10** — `nextra/mermaid` import path may exist in Nextra 4 but the project doesn't use it; `@theguild/remark-mermaid` is the configured renderer, wired through the MDX pipeline (not via `mdx-components.tsx`). The agent should NOT import from `nextra/mermaid`.

- **No new packages allowed** — the plan's Section 8 says "No new packages needed" and constraints forbid `package.json` changes.

- **Feature Gap sections only on pages with a listed gap** — per the plan's Risk Register: "Start with real observed gaps; add `<!-- GAP: -->` markers only when confirmed by a developer." Pages NOT in the Section 5 table (health/index, inspections/index, inspections/on-spot-inspection, web-admin/*, account/index, movements/death, movements/pasture, movements/slaughter, movements/market, etc.) should NOT include a Feature Gap section.

- **Ear Tag Orders is at `animals/ear-tag-orders.mdx`** — the folder structure (Section 3.2) puts it under `animals/`, which is authoritative over the ambiguous TODO item.

- **Diagram-to-page mapping inferred from content context** — 4.1 (Birth Registration) → `animals/birth-registration.mdx`; 4.2 (Ear Tag Order Lifecycle) → `animals/ear-tag-orders.mdx`; 4.3 (Movement Recording) → `movements/index.mdx` (it's an overview with deadline awareness, not a single workflow); 4.4 (Notifiable Disease) → `health/notifiable-diseases.mdx`; 4.5 (Inspection Risk Analysis) → `inspections/risk-analysis.mdx`; 4.6 (Cattle Passport Lifecycle) → `passports/index.mdx` (it's a lifecycle overview). No diagram is explicitly mapped to `passports/apply.mdx` or `inspections/index.mdx`.

- **The domain ADRs (ADR-0024 ear-tag, ADR-0025 movement) have state machines that differ from the plan's diagrams** — ADR-0025's actual ear-tag order state machine has 8 states (`DRAFT → PENDING → APPROVED → REJECTED → ORDERED → PARTIALLY_RECEIVED → RECEIVED → CANCELLED`), not the plan's simplified 6-state. ADR-0025's movement model has 16 `MOVEMENT_TYPE` values with linked-leg market patterns. The plan's diagrams are simplified views; actual domain complexity is deeper.

- **Mobile birth registration exists as `birth.tsx` and `create.tsx`** under `(tabs)/animals/`, and the offline queue exists in `providers/offline-provider.tsx` with `pendingCount`. The documented workflows can reference actual app screens.

VERIFIED-TOOLING
  (none verified)

## grill Q&A

Q1: The plan's Section 6 template requires import { Mermaid } from 'nextra/mermaid' and <Mermaid chart={...} />, but the project uses @theguild/remark-mermaid which renders from  mermaid  fenced code blocks and does NOT use a <Mermaid> component — should I follow the project's actual rendering pattern (fenced code blocks) or the plan's literal import/component template?
A1: Use fenced  mermaid  code blocks per the project's actual @theguild/remark-mermaid rendering pattern — NOT the plan's import { Mermaid } from 'nextra/mermaid' and <Mermaid chart={...} /> template. The nextra/mermaid component is not registered in this project's mdx-components.tsx, so following the plan's template would produce broken diagrams. (auto)

## spec

GOAL
Create the farmer and veterinarian user guide documentation section in the existing Nextra docs site (`apps/docs/`) by building the full directory tree of MDX pages and sub-navigation configs under `apps/docs/content/user-guide/` as specified in the plan at `plans/2026-07-20-farmer-vet-docs/plan.md`, editing only `apps/docs/content/_meta.ts` to insert the User Guide section after "Get Started" and before the "Learn" separator, and ensuring every workflow page includes the correct Mermaid diagram (using fenced ` ```mermaid ` code blocks per the project’s `@theguild/remark-mermaid` renderer — NOT the `nextra/mermaid` import), every page follows the defined template (title/sidebarTitle frontmatter, diagrams, sections, and `---` plus `### Feature Gap` only for pages that have a gap in the plan’s Section 5 table), and `pnpm build` passes with zero errors.

CONSTRAINTS
- Do not modify any existing file outside `apps/docs/content/user-guide/` except `apps/docs/content/_meta.ts`, which must be updated in-place to insert the User Guide section (a `'###user-guide': { type: 'separator', title: 'User Guide' }` entry followed by a `'user-guide': { type: 'menu', title: 'User Guide', items: { … } }` entry) after the `'get-started': 'Get Started'` line and before the `'###learn': { type: 'separator', title: 'Learn' }` line, preserving all existing entries, separators, and ordering.
- The `apps/docs/content/user-guide/_meta.ts` must match the plan's Section 3.3 verbatim — 10 entries (the `index` root page plus 9 top-level menu entries: `getting-started`, `animals`, `movements`, `health`, `passports`, `inspections`, `corrections`, `account`, `web-admin`). The `index` entry is the root page (not a `type: 'menu'` entry); the other 9 entries each have `title`, `type: 'menu'`, and `items` listing the child pages specified in Section 3.3.
- Every MDX page must use `title` and `sidebarTitle` in frontmatter. Pages that are section overviews (e.g. `animals/index.mdx`) must also include `asIndexPage: true` per the project’s existing pattern.
- Use fenced ` ```mermaid ` code blocks for all diagrams — do NOT import `Mermaid` from `nextra/mermaid` or use `<Mermaid chart={...} />`. The project renders diagrams via `@theguild/remark-mermaid`, which supports standard fenced code blocks only.
- Feature Gap sections (`---` then `### Feature Gap`) must ONLY be placed on pages that have a corresponding gap entry in the plan’s Section 5 table. Pages without a listed gap (e.g., `health/index.mdx`, `inspections/index.mdx`, all `web-admin/*` pages, `movements/death.mdx`, etc.) must omit the Feature Gap section entirely.
- `ear-tag-orders.mdx` lives at `apps/docs/content/user-guide/animals/ear-tag-orders.mdx` (the folder structure of Section 3.2 is authoritative over the ambiguous TODO item).
- Diagram-to-page mapping: Section 4.1 (Birth Registration)→`animals/birth-registration.mdx`; 4.2 (Ear Tag Order Lifecycle)→`animals/ear-tag-orders.mdx`; 4.3 (Movement Recording)→`movements/index.mdx`; 4.4 (Notifiable Disease)→`health/notifiable-diseases.mdx`; 4.5 (Inspection Risk Analysis)→`inspections/risk-analysis.mdx`; 4.6 (Cattle Passport Lifecycle)→`passports/index.mdx`.
- All internal doc links must use absolute paths from `content/` (e.g., `/how-to/use-mermaid-diagrams`) — no `../` escape paths — to pass `pnpm check:md-links`.
- Do not add any new npm packages or dependencies. Do not modify any `package.json` files. Preserve every existing dependency, devDependency, script, and field.
- All content must be in English. Macedonian translation is a follow-up, not part of this task.
- The plan's diagrams are simplified views of actual domain state machines documented in ADRs; implement the diagrams as written in the plan — do not enrich them with ADR details.

ACCEPTANCE
- Directory `apps/docs/content/user-guide/` and all subdirectories (`getting-started/`, `animals/`, `movements/`, `health/`, `passports/`, `inspections/`, `corrections/`, `account/`, `web-admin/`) exist with exactly the MDX files and `_meta.ts` files listed in the plan's Section 3.2.
- `apps/docs/content/_meta.ts` is updated with the User Guide separator and menu inserted in the correct position, and all pre-existing entries remain unchanged.
- `apps/docs/content/user-guide/_meta.ts` matches the plan's Section 3.3 exactly — every key, title, type, and items structure (10 entries: `index` root page plus 9 menu entries).
- Each workflow page includes the correct fenced ` ```mermaid ` diagram from the plan's Section 4.
- Every page with a gap listed in the plan's Section 5 includes a `<!-- GAP-XXX: description -->` comment and a matching table row in its `### Feature Gap` section.
- Every page follows the template: frontmatter with `title` and `sidebarTitle` (and `asIndexPage: true` for index pages), then `# Page Title`, then the diagram (if applicable) as a fenced ` ```mermaid ` block, then `## When to Do This`, `## Requirements`, `## Step-by-Step`, `## What Happens Next`, then `---` and `### Feature Gap` only for pages with gaps.
- `pnpm --filter docs build` completes with zero errors.
- `pnpm --filter docs check:md-links` passes with no broken internal links.

VERIFY:
```sh
# Verify all required directories exist
for dir in getting-started animals movements health passports inspections corrections account web-admin; do
  test -d "apps/docs/content/user-guide/$dir" || { echo "Missing directory: $dir"; exit 1; }
done
# Verify _meta.ts files exist in every subdirectory
for dir in . getting-started animals movements health passports inspections corrections account web-admin; do
  test -f "apps/docs/content/user-guide/$dir/_meta.ts" || { echo "Missing _meta.ts in $dir"; exit 1; }
done
# Verify key content files exist (one per section that has workflow pages)
for f in animals/birth-registration.mdx animals/ear-tag-orders.mdx movements/index.mdx health/notifiable-diseases.mdx inspections/risk-analysis.mdx passports/index.mdx; do
  test -f "apps/docs/content/user-guide/$f" || { echo "Missing file: $f"; exit 1; }
done
# Build and link check
pnpm --filter docs build && pnpm --filter docs check:md-links && echo "All checks passed"
```

## phase timings

refine              123.4s
research            931.5s
  enrichment              4.3s
  worker:files wait       1.5s
  worker:files work     146.6s
  worker:apis wait        1.8s
  worker:apis work      217.3s
  worker:context wait     1.7s
  worker:context work   182.3s
  worker:tooling wait     1.6s
  worker:tooling work   104.8s
  workers               901.9s
  verify-tooling         29.6s
grill               140.4s
  gen                    26.4s
  auto-answer            47.8s
  gen                    39.3s
  gen                    26.9s
compose              34.6s
critique            155.0s
  triage                 84.9s
  rewrite                70.0s
total              1384.9s

## handoff

handoff_at: 2026-07-20T19:01:51.848Z
