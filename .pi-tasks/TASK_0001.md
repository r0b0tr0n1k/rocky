---
id: TASK_0001
state: failed
phase: research
created_at: 2026-07-20T15:15:06.077Z
updated_at: 2026-07-20T15:48:15.590Z
title: Implement the ADR × work-order integration plan documented in `.pi/plans/2026-07-20-adr-integration/plan.md` (item-level acceptance in `todos.md`, tag `adr-integration`, authoritative map `scout-context.md`), closing the eight engineering-ready seams where an architecture decision's producer is built but its consumer is missing or partial. Done means: every `adr-integration`-tagged todo is completed (the five doc/ADR fixes of Workstream 1, the geo consumption of WS2, the ART 19(4) replacement-tag guard of WS3, the IoT `RuleSet.features.iot` gate of WS4, the audit-coverage extension of WS5, the `@rocky/observability` scaffold plus `ExecutionPipeline` wiring of WS6, the i18n exposure of WS7, and the PDF verify loop of WS8); the three flagged open questions are resolved using the planner's recommended defaults (OQ-1: Movement-only geo, Inspection left out of scope; OQ-2: `ExecutionEventEmitter` retired from the definition of done but code kept, telemetry routed through `TraceStage`/`MetricsStage`; OQ-3: `next-intl` for web and `i18next` for mob); and the repo passes `pnpm check:adrs`, `pnpm check:md-links`, `pnpm check:layers`, `pnpm check:shadcn`, `ci:checks`, and `pnpm build`.
label: ADR × work-order integration: close 8 seams across 8 workstreams
reason: Research CONTEXT worker produced no output
---

## raw prompt

read :1-90



 read


 Thinking...

 Plan and todos are written. Here's the review.

 Plan: closing the ADR × workorder integration seams

 Artifacts: plan.md (270 lines) + todos.md (23 items, tag adr-integration) at
 .pi/plans/2026-07-20-adr-integration/.

 8 workstreams, phased by leverage → risk:
 1. Doc/ADR reconciliation (5 fixes) — free, unblocks check:adrs/check:md-links
 2. Geo consumption (ADR-0078) — Farm boundary query + dashboard geo-count tiles
 3. Traceability ART 19(4) (ADR-0085) — one missing replacement-tag guard
 4. IoT RuleSet.features.iot gate (WO-060) — features dim + policy capability + router/UI guards
 5. Audit coverage extension (WO-159 follow-up) — wire AuditService into
    inspection/correction/notification/iot/archive
 6. Observability scaffold (WO-034/035/036) — new @rocky/observability + pipeline stages
 7. i18n (WO-086) — session.language exposure + next-intl (web) / i18next (mob)
 8. PDF verify loop (ADR-0082/0084) — EmbedPDF on /verify + mobile QR→verifyCredential

 Excluded (not engineering-ready): WO-124–142 (gov), WO-133–141 (legal), 10 Proposed ADRs, and
 deferred-by-design items (WO-061/070, audit partitioning, ADR-0084 Phase-3, ADR-0087 Phase-0,
 WO-025/097/161/024).

 Three OPEN QUESTIONS the planner flagged (each with a recommended default — your call before code):
 - OQ-1 Inspection geo: Movement-only (leave Inspection out) ✅ recommended
 - OQ-2 ExecutionEventEmitter: retire-from-DoD, keep code, telemetry via Trace/Metrics stages ✅
   recommended
 - OQ-3 i18n libs: next-intl (web) + i18next (mob) — flagged for confirmation - we should accept the open question proposals and plan for the stages

## refined prompt

GOAL
  Implement the ADR × work-order integration plan documented in `.pi/plans/2026-07-20-adr-integration/plan.md` (item-level acceptance in `todos.md`, tag `adr-integration`, authoritative map `scout-context.md`), closing the eight engineering-ready seams where an architecture decision's producer is built but its consumer is missing or partial. Done means: every `adr-integration`-tagged todo is completed (the five doc/ADR fixes of Workstream 1, the geo consumption of WS2, the ART 19(4) replacement-tag guard of WS3, the IoT `RuleSet.features.iot` gate of WS4, the audit-coverage extension of WS5, the `@rocky/observability` scaffold plus `ExecutionPipeline` wiring of WS6, the i18n exposure of WS7, and the PDF verify loop of WS8); the three flagged open questions are resolved using the planner's recommended defaults (OQ-1: Movement-only geo, Inspection left out of scope; OQ-2: `ExecutionEventEmitter` retired from the definition of done but code kept, telemetry routed through `TraceStage`/`MetricsStage`; OQ-3: `next-intl` for web and `i18next` for mob); and the repo passes `pnpm check:adrs`, `pnpm check:md-links`, `pnpm check:layers`, `pnpm check:shadcn`, `ci:checks`, and `pnpm build`.

CONSTRAINTS
  - Treat `plan.md`, `todos.md`, and `scout-context.md` at `.pi/plans/2026-07-20-adr-integration/` as authoritative; preserve every concrete identifier they pin verbatim — file paths, `apps/api/src/app.module.ts` line refs, method names (`getHoldingBoundary`, `countActiveDiseaseZones`, `countOpenGeofenceEvents`, `replaceTag`, `runDiseaseZoneCheck`, `findActiveDiseaseZonesNearFarm`, `recordCreate/update/delete`, `verifyCredential`), ADR/WO numbers, enum tokens (`ART19_DUAL_CODE_ON_REPLACEMENT`), the `fenceType = DISEASE_ZONE` / `isActive` filter, and types (`RuleSetFeatures`, `PolicyMetadata`, `ExecutionStage`, `TraceStage`, `MetricsStage`), plus the package name `@rocky/observability`.
  - Do NOT implement anything in Excluded Scope: WO-124–142 (gov ratification), WO-133–141 (GDPR/legal), the 10 Proposed ADRs (0061/0065/0071/0073/0074/0081/0093/0106/0107/0109), and deferred-by-design items WO-061/070, audit native RANGE partitioning, ADR-0084 Phase-3 visual render, ADR-0087 Phase-0 GLN backfill, WO-024, WO-025, WO-097, WO-161. ADR-0107 may only be marked Superseded by ADR-0082/0084 or converted to a fresh Proposed template-catalog ADR per WS1.3 — not silently flipped.
  - Preserve all existing `package.json` entries (dependencies, devDependencies, scripts, `exports`, tsconfig options) across the monorepo; this is an in-place update that adds only the task's explicit delta. `@rocky/observability` is the single allowed NEW package and must satisfy `check:layers` (no forbidden imports into `@rocky/database`/`@rocky/events`/`@rocky/validators/api+events`) and `check:shadcn`.
  - Keep `ExecutionEventEmitter` code and its existing `emit()` calls intact (per OQ-2); do not delete it and do not build telemetry on top of it.
  - Audit wiring (WS5) must mirror the existing `FarmService` pattern exactly: optional `auditService?` ctor param, `this.auditService?.recordCreate/update/delete(...)` at mutating points; `features.iot` defaults to `true` to avoid breaking current behavior; the IoT service records only at `registerDevice` — never per `ingestReading`/`ingestReadings`.
  - Do not change business logic beyond what each workstream specifies; the observability stages (WS6.2) and the OQ-2 DoD retirement must introduce no behavior change to pipelined requests beyond emitting a span + metric.
  - Respect the pinned `app.module.ts` wiring exactly: `FarmService(repo, auditService, undefined, geoService)` at ~`:280`; InspectionService at ~`:373`; NotificationService at ~`:326`; CorrectionService at ~`:405`; `IotService(repo, auditService)` at ~`:416`; ArchiveService at ~`:383`; `GeoRouter` already registered at ~`:583`; the IOT `modules` seed row at `packages/database/src/seed.ts:~623`.
  - Update the owning `AGENTS.md` for any package whose contract changes (`packages/execution` pipeline stages, `packages/authorization` `feature` policy, `packages/domains/system` `features`).

KNOWN-UNKNOWNS
  - The three open questions are explicitly "flagged for confirmation before implementing." Must the user confirm OQ-1/OQ-2/OQ-3, or are the recommended defaults pre-authorized? Confirm before writing code — especially OQ-3, a long-lived library decision.
  - Which native QR-scanner dependency for WS8.2 — `expo-camera` (Expo SDK, likely already present) vs `react-native-vision-camera`? Confirm availability/license; it is a NEW mobile dependency to flag in the PR.
  - Does `animalGeofenceEvents` actually have an `acknowledgedAt`/`status` column (WS2.2)? If absent, "open geofence events" = events in the last N days (note in PR) — verify the schema in `packages/database/src/schema/geo/*` before writing the count query.
  - Does the user/session schema already carry `language` (WS7.1), and is `IOT_ENABLED` a real system parameter (WS4.1)? If the `language` column or `IOT_ENABLED` param is absent, a DB migration + seed change is required (follow the `packages/database` migration workflow); default locale value is `EN`.
  - Is `@opentelemetry/api` present in the pnpm `catalog:` (WS6.1)? If absent, add as optional/peer and degrade to a no-op shim; confirm before wiring `TraceStage`/`MetricsStage`.
  - ADR-0078's dashboard geo-count Consequence (WS1.5) must be updated to match shipped behavior only after WS2.2 lands — confirm ordering so the doc fix does not pre-empt the implementation.
  - Status-list freshness handling for the mobile verify loop (WS8.2) — confirm how the web's pinned public key and status-list staleness warning are reused on RN.
  - Confirm `trpc.document.verify`, `document.credential`, `document.statusList`, and `document.verifyCredential` are already generated/exported (plan states they are) before consuming them in WS8.

EXTERNAL-DEPENDENCIES
  - MapTiler  MapTiler Maps/Geocoding REST API consumed via @maptiler/client for disease-zone checks and farm holding-boundary queries (GeoService)
  - OpenTelemetry  OpenTelemetry API (tracer.startActiveSpan / metrics) used by TraceStage and MetricsStage in @rocky/observability

## research worker FILES

.pi/plans/2026-07-20-adr-integration/plan.md  authoritative plan: 8 workstreams, constraints, excluded scope, open questions
.pi/plans/2026-07-20-adr-integration/todos.md  item-level acceptance for tag `adr-integration`
.pi/plans/2026-07-20-adr-integration/scout-context.md  authoritative ADR×workorder seam map
apps/docs/content/ADR/0007-audit-via-lifecycle-events.md  WS1.1 re-label Status: Superseded / Superseded: 0012
apps/docs/content/ADR/0064-disease-zone-spatial-block.md  WS1.2 fix IotRepository→GeoService text
apps/docs/content/ADR/0107-pdf-template-system.md  WS1.3 mark Superseded by ADR-0082/0084
apps/docs/content/workorder.md  WS1.4 clarify WO-050 vs ADR-0082/0084 Phase-3
apps/docs/content/ADR/0078-geo-spatial-service.md  WS1.5 update dashboard geo-count Consequence after WS2.2
scripts/check-adrs.mjs  guardian gate for WS1 ADR header format (ADR-0033)
scripts/check-md-links.mjs  guardian gate for WS1 doc links
packages/domains/farm/src/services/farm.service.ts  WS2.1 add geoService ctor param + getHoldingBoundary(farmId)
packages/domains/farm/src/index.ts  WS2.1 re-export getHoldingBoundary if needed
apps/api/src/app.module.ts  WS2.1 FarmService(~:280)+GeoService inject; WS5 Inspection(:373)/Correction(:405)/Notification(:326)/Iot(:416)/Archive(:383) audit wiring; WS6.2 TraceStage/MetricsStage provide+inject; GeoRouter(:583)
packages/geo/src/services/geo.service.ts  WS2.2 add countActiveDiseaseZones() + countOpenGeofenceEvents()
packages/geo/src/repositories/geo.repository.ts  WS2.2 count queries against geofences/animalGeofenceEvents
apps/api/src/routers/geo.router.ts  WS2.2 add geo.countActiveDiseaseZones + geo.countOpenGeofenceEvents @Query
apps/web/components/dashboard/analytics.tsx  WS2.2 add geo counts to useDashboardData via trpc.geo.*
apps/web/app/(admin)/dashboard/page.tsx  WS2.2 render two /geo-linked count Cards; WS7.2 i18n first route
apps/web/components/dashboard/stat-tile.tsx  WS2.2 reuse for count tiles
packages/database/src/schema/an/geofences.ts  WS2.2 schema ref: has fenceType + isActive (countActiveDiseaseZones filter)
packages/database/src/schema/an/animal-geofence-events.ts  WS2.2 schema ref: NO acknowledgedAt/status — open = events in last N days
packages/domains/eartag/src/services/eartag.service.ts  WS3.1 add replaceTag(...) ART19_DUAL_CODE_ON_REPLACEMENT guard
packages/domains/eartag/src/services/eartag.service.test.ts  WS3.1 unit test enabled/disabled + dual/single code
packages/validators/src/api/eartags.api.ts  WS3.1 add replaceTagRequestSchema (visual+electronic code)
apps/api/src/routers/eartag.router.ts  WS3.1 add eartag.replaceTag mutation + EARTAG_TRPC_ERROR_MAP entry
packages/domains/system/src/traceability-rules.ts  WS3.1 ref: isEnabled(), requiresDualCodeRecording(), ART19_DUAL_CODE_ON_REPLACEMENT token
packages/validators/src/utils/check-digit.ts  WS3.1 ref: makeEarTagSchema(format)
packages/domains/eartag/src/errors/eartag.errors.ts  WS3.1 ref: EAR_TAG_ERRORS.INVALID_INPUT
packages/domains/system/src/rule-set.ts  WS4.1 add features:{iot} to RuleSet + buildRuleSet; export RuleSetFeatures
packages/database/src/seed.ts  WS4.1 IOT module seed row isActive drives features.iot (line ~623)
packages/authorization/src/policies/policy.decorator.ts  WS4.2 add feature?:string to PolicyMetadata
packages/authorization/src/policies/engine.ts  WS4.2 evaluate() deny when ruleSet.features[policy.feature] falsy
packages/authorization/src/policies/policy-engine.test.ts  WS4.2 cover feature enabled/disabled
apps/api/src/routers/iot.router.ts  WS4.3 add @Policy({authenticated:true,feature:"iot"})
apps/api/src/routers/device.router.ts  WS4.3 add @Policy({authenticated:true,feature:"iot"})
apps/web/app/(admin)/iot/page.tsx  WS4.3 render off-state when features.iot=false
apps/web/app/(admin)/devices/  WS4.3 guard device pages on features.iot (page.tsx, new/, [id]/edit/)
apps/api/src/routers/system-parameters.router.ts  WS4.3 expose features via system router if not already present
packages/domains/inspection/src/services/inspection.service.ts  WS5.1 add auditService? ctor + record at create/update/complete/cancel
packages/domains/inspection/src/services/risk-analysis.service.ts  WS5.1 add auditService? + record at runRiskAnalysis
packages/domains/inspection/src/services/inspection.service.test.ts  WS5.1 assert recordCreate/update called
packages/domains/correction/src/services/correction.service.ts  WS5.2 add auditService? + record at createCorrection/applyCorrection
packages/domains/correction/src/services/correction.service.test.ts  WS5.2 assert audit calls
packages/domains/notification/src/services/notification.service.ts  WS5.3 add auditService? + record at send/create
packages/domains/notification/src/services/subscription-resolver.service.ts  WS5.3 add auditService? + record at subscription CUD
packages/domains/notification/src/services/channel-router.ts  WS5.3 add auditService?
packages/domains/notification/src/services/notification.service.test.ts  WS5.3 tests updated
packages/domains/iot/src/services/iot.service.ts  WS5.4 add auditService? + record ONLY at registerDevice
packages/domains/iot/src/services/iot.service.test.ts  WS5.4 assert registerDevice audited, ingest NOT
packages/domains/archive/src/services/archive.service.ts  WS5.5 add auditService? + record at archiveDocument/markDestroyed
packages/domains/archive/src/services/archive.service.test.ts  WS5.5 tests updated
packages/domains/audit/src/services/audit.service.ts  WS5 ref: AuditService.recordCreate/update/delete consumed by 5 services
packages/authorization/src/permissions.drift.test.ts  WS5 cross-cut: mirror audited-domain coverage assertion test
packages/observability/package.json  WS6.1 NEW package @rocky/observability (exports/index/build)
packages/observability/tsconfig.json  WS6.1 NEW (mirror packages/execution)
packages/observability/src/index.ts  WS6.1 NEW exports TraceStage/MetricsStage/attributes
packages/observability/src/trace.stage.ts  WS6.1 NEW TraceStage implements ExecutionStage (OTel startActiveSpan w/ no-op shim)
packages/observability/src/metrics.stage.ts  WS6.1 NEW MetricsStage implements ExecutionStage (BusinessMetrics recorder)
packages/observability/src/attributes.ts  WS6.1 NEW domain Result/error → span attributes
packages/observability/README.md  WS6.3 document logger.*→spans/metrics pattern
packages/execution/src/execution-pipeline.ts  WS6.2 ctor accept traceStage/metricsStage; wrap handler in rlsStage.run
packages/execution/src/execution.module.ts  WS6.2 provide TraceStage/MetricsStage via useFactory; inject into ExecutionPipeline
packages/execution/src/index.ts  WS6.2 export ExecutionStage + new stages if needed
packages/execution/src/events/event-emitter.ts  OQ-2 keep ExecutionEventEmitter + emit() calls intact (no telemetry)
packages/domains/movement/src/services/movement.service.ts  WS6.3 replace ad-hoc logger.* with span attrs + emit movement.created / movement.blocked_disease_zone
packages/execution/AGENTS.md  WS6 update pipeline-stage contract doc
packages/authorization/AGENTS.md  WS4 update feature policy contract doc
packages/domains/system/AGENTS.md  WS4 update (file does not currently exist — create/align per plan)
pnpm-workspace.yaml  WS6.1/WS7: catalog lacks @opentelemetry/api, next-intl, i18next, react-i18next, expo-camera / react-native-vision-camera — must add as optional/peer
apps/api/src/auth/auth.ts  WS7.1 customSession expose language (add user/session language column default EN if absent)
packages/auth/src/better-auth.ts  WS7.1 ensure session type carries language
packages/database/src/schema/auth/  WS7.1 verify/ add language column on user/session schema if absent (migration+seed)
apps/web/app/layout.tsx  WS7.2 wrap root with NextIntlClientProvider, set <html dir> from locale
apps/web/i18n/  WS7.2 NEW next-intl config
apps/web/messages/  WS7.2 NEW en.json (+mk.json stub)
apps/web/package.json  WS7.2 add next-intl dependency
apps/mob/app/_layout.tsx  WS7.3 wrap with i18next provider
apps/mob/lib/i18n.ts  WS7.3 NEW i18next config
apps/mob/locales/  WS7.3 NEW en.json (+mk.json)
apps/mob/lib/auth.ts  WS7.3 expose mobile session language; WS8.2 trpc doc client
apps/mob/package.json  WS7.3 add i18next+react-i18next; WS8.2 add native QR scanner dep
apps/mob/app/(tabs)/index.tsx  WS7.3 pattern screen — replace hardcoded strings
apps/web/components/pdf/drop-in-viewer.tsx  WS8.1 reuse EmbedPDF viewer on /verify
apps/web/app/(admin)/verify/page.tsx  WS8.1 mount viewer after document.verify (next/dynamic ssr:false)
apps/api/src/routers/document.router.ts  WS8 ref: document.verify/credential/statusList already implemented
packages/trpc/src/generated/server.ts  WS8 ref: document.verifyCredential publicProcedure present (line ~159)
packages/pdf/src/index.ts  WS8 ref: verifyCredential + CredentialVerifyView exported
packages/pdf/src/services/credential.service.ts  WS8 ref: CredentialVerifyView / CredentialOutput types
apps/mob/app/(tabs)/verify.tsx  WS8.2 NEW QR-scan screen → trpc.document.verifyCredential.query({qr})
scripts/check-layers.mjs  guardian gate: observability must not import forbidden pkgs
scripts/check-shadcn-primitives.mjs  guardian gate: no duplicate UI primitives in observability/web
scripts/check-trpc-boundary.mjs  ci:checks gate
scripts/check-web-parity.mjs  ci:checks gate
scripts/check-standards.mjs  ci:checks gate
scripts/check-agents.mjs  ci:checks gate
packages/execution/tsconfig.build.json  WS6.1 mirror for new observability package tsconfig
packages/geo/src/errors/geo.errors.ts  WS2.2 ref: GEO_ERRORS for count methods

## research worker APIS

(degraded: research APIS worker timed out after restarts; this section may be incomplete)

Here are the 3 session markers from this conversation:

- **marker-1:** `8441b2024e7e4724b37aafd798c2d1b4`
- **marker-2:** `f77cadc0e9d1444396844f565921d8a4`
- **marker-3:** `2af83ecf1c4347228433c765760d0459`

(Recall verified — these match the values listed in markers 1/3, 2/3, and 3/3 above.)

## phase timings

refine              241.6s
research           1719.5s
  enrichment              2.0s
  worker:files wait       1.9s
  worker:files work     186.7s
  worker:apis wait        1.8s
  worker:apis work      238.2s
  worker:context wait    11.3s
  worker:context work    77.5s
total              1961.1s
