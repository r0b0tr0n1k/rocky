import type { Meta } from "nextra";

const meta: Meta = {
  "validation-circle-type-safety": "The Validation Circle — Type Safety",
  "system-architecture": "System Architecture (C4 + UML)",
  "better-auth-and-cloudflare-access": "Better Auth & Cloudflare Access",
};

export default meta;
