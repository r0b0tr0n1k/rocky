# Macedonian LPDP + GDPR + ISO/IEC 27701:2025 Controls Mapping Matrix (retired crosswalk)

> **Retired as a duplicate crosswalk.** This document previously carried the
> Macedonian LPDP ↔ GDPR ↔ ISO/IEC 27701:2025 ↔ ISO/IEC 27001:2022 ↔ SCF ↔
> implementation ↔ evidence matrix as a standalone mapping.
> The canonical, machine-checked crosswalk for Rocky is now
> [`VALIDATED_CROSSWALK`](../../../../packages/validators/src/compliance/gdpr-articles.ts)
> in `packages/validators/src/compliance/gdpr-articles.ts` (GDPR ↔ MK LPDP ↔ AL Law 124
> ↔ ISO/IEC 27701:2025). Each row in `VALIDATED_CROSSWALK` is an engineering reference,
> not legal advice; its `iso27701` column is machine-derived and **pending expert review**.
>
> The human-readable, in-scope Macedonian jurisdiction treatment remains
> [`MACEDONIAN_LPDP.md`](../compliance/MACEDONIAN_LPDP.md). The control-by-control status
> of each mapped control lives in the canonical Statement of Applicability,
> [`isms-policy.md`](../compliance/isms-policy.md) (ROCKY-ISMS-001).

This file is kept only so existing inbound links resolve. Do not treat it as the
source of truth for any crosswalk — cite `VALIDATED_CROSSWALK` instead.
